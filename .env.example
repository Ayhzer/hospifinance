# Configuration de l'API backend (optionnel)
# Si vide ou absent : mode localStorage + GitHub (recommandé)
# Si défini : mode API (nécessite un backend Express + MongoDB)
# VITE_API_URL=http://localhost:3001/api

# Synchronisation GitHub automatique
# Ces variables activent la sync GitHub sans configuration manuelle dans le navigateur.
# VITE_GITHUB_TOKEN=ghp_xxxxxxxxxxxxx   (Fine-grained PAT avec accès au repo de données)
# VITE_GITHUB_OWNER=ayhzer
# VITE_GITHUB_REPO=hospifinance_data
# VITE_GITHUB_BRANCH=main
# VITE_GITHUB_DATA_PATH=data
