# Configuration de l'API backend pour la production
# API Render désactivée - stockage via localStorage + GitHub
# VITE_API_URL=https://hospifinance.onrender.com/api

# Synchronisation GitHub automatique (injectée au build via GitHub Actions secrets)
# Ces valeurs sont remplacées par les secrets GitHub lors du déploiement.
# NE PAS mettre le token ici — il est injecté via le secret VITE_GITHUB_TOKEN.
VITE_GITHUB_OWNER=ayhzer
VITE_GITHUB_REPO=hospifinance_data
VITE_GITHUB_BRANCH=main
VITE_GITHUB_DATA_PATH=data
